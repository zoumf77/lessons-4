1：可以收集一下自己碰到的注解，看看如何使用元注解定义的；
2：枚举
用枚举描述一个运动员跑步的状态；并举例使用；
用枚举描述一年12月，要包括月份和天数；并举例使用；
使用name()和 values()，总结一下他们返回值是什么？

4：试试看说明一个变量List<?> li,
li.add(Obect o),li.add(null)会有什么反应.请说明原因？

5：泛型檫除是怎么定义的？在什么时候檫除的？建议调试一下，在调试栈里看看调用的方法的参数是怎么样的。

6：了解JVM的内存结构，栈帧、堆、方法区的区别与联系
